# Retrieval benchmark v1 (frozen file)

Checkpoint: `c0e7381c3c67cf95edfaff0c5dab201c9fa8a1c2` on `checkpoint/2026-08-13-architecture-baseline`.

**Status:** `owner_review_status=provisional_assistant` for every row until the owner freezes development and promotion.

Canonical pack: `benchmarks/retrieval/v1.jsonl`
SHA-256 (canonical JSONL, sorted keys, one object per line): `00f632ef0f296f23bb81226dc9fd921f5c17b3c2511edc02e03e043e4a3f583b`

The promotion loader in `backend/app/retrieval/benchmark.py` still reads `benchmarks/retrieval/v1.json`. That file remains **draft with empty queries** and points here. Do not mark it `approved` in this task.

## Splits

| Split | Count | Use |
|---|---:|---|
| development | 16 | Tuning and error analysis |
| promotion | 8 | Frozen gate after owner review |
| adversarial_holdout | 8 | Do **not** open until the candidate is frozen. Runners must not use holdout for tuning. |

Holdout query text is stored in `v1.jsonl` with `split=adversarial_holdout`. The OWNER-VIEW redacts holdout query prose.

## Families (16)

All 16 appear in development. Promotion/holdout reuse some families on **different** sources and wording.

1. `exact_statute`
2. `case_name`
3. `id_only` (development only)
4. `entity_substitution` (development only)
5. `faithful_paraphrase`
6. `mixed_lexical_semantic`
7. `terminology_synonym`
8. `multi_authority`
9. `currentness`
10. `source_span_provenance`
11. `negative_out_of_scope`
12. `wrong_jurisdiction`
13. `clarification`
14. `evidence_limited`
15. `teaching_not_authority`
16. `zero_hit`

Promotion and holdout are **not** ID-only, entity-substitution, or paraphrase variants of development queries.

## Gold

Positive rows bind `expected_source_id` and `proposition_anchor` from approved catalogue material:

- current/latest-available legislation (`*:latest-available@2026-08-12`, `currentness_status=point_in_time`, OGL v3.0)
- OGL UKSC PDFs (`neutral-citation:[…] UKSC …`)

Do not invent gold. Negative rows have `expected_behavior` in `no-answer` / `clarification` / `wrong-jurisdiction` / `evidence-limited` and **no gold answer prose**.

Teaching and assessment lanes are forbidden. Find Case Law full text remains excluded.

## Leakage

See `v1.summary.json` → `leakage`. Findings: [].

## Owner legal confirmation

Rows with `legal_confirmation_needed=true` still need owner review of the expected source / proposition mapping (especially UKSC paragraph locators and multi-section pairs). All 32 rows stay `provisional_assistant` until freeze.

## Non-ingestible owner view

`v1.OWNER-VIEW.md` is an owner review pack. It is **not** corpus, RAG, training, or eval-retrieval material.
