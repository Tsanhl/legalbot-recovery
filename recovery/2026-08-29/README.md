# LegalBot v1.11 recovery custody

This directory retains only the minimum recovery material needed beside the
clean application workspace. It is not an active Git repository, candidate,
index or execution authority.

- `final-manifest/` records the fail-closed 29 August disaster recovery.
- `receipts/` binds the recovered catalogue, sources, evaluation roots,
  seminar-config evidence and non-runnable candidate metadata.
- `recovered-ten-commit-history.bundle` preserves historical Git objects and
  refs. Do not restore or merge it into the owner's new repository without an
  explicit instruction.
- `CLEANUP-MANIFEST.json` and `WORKSPACE-CLEANUP-REPORT.md` record the verified
  flattening, recoverable Trash moves and the one remaining Integration-path
  cleanup hold.
- `catalogue-backups/` retains one current catalogue backup and the immediate
  predecessor required by the backup policy.
- `source-backup/` is a copy-on-write snapshot of the 316 recovered source
  files. Git intentionally ignores it and the private catalogue backups.

The current workspace must remain non-ACTIVE. Phase 1 rebaseline work is listed
in `docs/status/V111_PHASE1_RECOVERY_READINESS_20260829.md`.

The damaged `LegalBot-v111-Integration` path is not a recovery input. It can be
moved to recoverable Trash only after every Codex/ChatGPT process using it as a
working directory has closed.
