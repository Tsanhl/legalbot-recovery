# LegalBot v1.11 recovery-workspace agent rules

## Workspace authority

- `/Users/hltsang/Desktop/LegalBot-v111-Recovery-20260829` is the only writable v1.11 implementation workspace.
- `/Users/hltsang/Desktop/LegalBot-New` is outside this workspace. Do not read, edit, format, generate artifacts in, initialize or alter Git in, commit, rebase, clean, stash or otherwise access or mutate it without a new explicit owner authorization.
- `/Users/hltsang/Desktop/LegalBot-v111-Integration` is a damaged retired path. Do not import, execute, write, or recover from it. It may only be moved to recoverable Trash after every process using it has stopped.
- Git metadata is present as observed on 31 August 2026; the earlier no-Git cleanup state is historical. Its presence is not authorization to mutate Git. Do not initialize Git, add a remote, create a branch, commit, push or restore the historical bundle unless the owner explicitly authorizes that exact action.
- Do not promote a candidate, run Owner Certification 60, unseal Validation 30 or activate live without the applicable owner gate.
- Keep Phase 2A and all later phase implementation and execution in this repository. Do not create a sibling top-level repository or clone for an individual phase.
- Store new runtime/evaluation phase evidence beneath this repository and use concise phase/date run names. Add `-rN` only when a new immutable execution artifact is required. The working design is edited in place and does not receive automatic archive packs.
- Keep `README.md`, this `AGENTS.md`, `docs/CURRENT_STATE.md` and the working system-design files as maintained current documents.
- The latest 31 August owner direction uses exactly three delivery phases: (1) system design, (2) evaluation -> training/improvement -> unseen testing, General Enquiries first and PB/Essay retained, (3) live last. `docs/V111_SYSTEM_DESIGN.md`, `docs/V111_RELEASE_ROADMAP.md` and `docs/V111_REBUILD_CHECKLIST.md` map the work and retained exact controls inside those phases. Preparing the full current GE visible/unseen draft packages does not authorize answer runs, training, validation disclosure, promotion or live activation. Preserve existing package identities and private unseen custody.
- On 1 September 2026 the owner accepted the full current system-design amendments and directed that the design remain simple and editable rather than frozen into repeated versions. The workflow is: owner request -> Codex improves and prepares the plan/result -> Codex asks only for necessary owner decisions -> owner approves -> Codex completes the work. Exact runtime and evaluation artifacts still bind versions/digests for reproducibility.
- The owner also accepted the GE-visible-r3 wording/review specification. This does not approve legal gold, currentness, source admission, model use, Phase-2 execution, training, unseen disclosure/run, promotion or live activation.
- On 2 September 2026 the owner recorded RETURN_FOR_REVISION for the diagnostic 331+60 full-review r2 pack. Preserve that pack. Classify its 60 diagnostic cases as exposed regression material. Continue evaluator/retrieval/answer and non-weight planner repair only. Prepare official-source and currentness packets. Do not run answer-weight training, open sealed unseen material, promote, or activate live.
- The 2 September 2026 r2 overlay is an AI-assisted owner-advisory research decision, not qualified England-and-Wales legal review, gold, runtime admission or training approval. Official create-only staging intake, official page retrieval, immutable capture/hashing, point-in-time/effects review and case-route mapping are authorized. Do not set `admitted=true`, `full_current_law_eligible=true`, `qualified_legal_review=true` or `legal_gold=true` on fetch. Do not rerun the 331, train weights, disclose unseen, promote or activate live on this overlay. Do not admit an unidentified “Mediation Act 2025”.
- On 2 September 2026 the owner recorded OWNER_ADOPTION of that exact overlay as an owner-adopted research and process decision. Preserve the overlay and the adoption receipt. Continue only the authorized next work. Do not treat owner adoption as qualified legal review, gold, admission, weight training, unseen, promotion or live.
- The owner then authorized execution of that next work: official create-only staging intake, point-in-time/extent/commencement/effects review, proposition mapping, and evaluator/retrieval/non-weight planner repair.
- On 2 September 2026 the owner authorized a visible 331 diagnostic evaluation rerun under the factual-first gate. This remains Phase 2 evaluation. The owner’s fact-check approval is the evaluation method; it does not set `qualified_legal_review`, `legal_gold`, `admitted`, `full_current_law_eligible`, answer-weight training, sealed unseen, promotion or live. Do not mint a fresh unseen set or open the 306 private bank.
- A per-locator gold DRAFT exists as a historical all-PENDING artifact. On 2 September 2026 the owner supplied the resolved 67-locator evaluation-gold package (66 APPROVE, 0 HOLD, 1 REJECT). That is locator-level evaluation gold only, not qualified legal review, answer gold, runtime admission, weight training, unseen, promotion or live. Do not retick the unsigned draft. Cable & Wireless is REJECT from the mandatory evaluation evidence route and stays fail-closed after Find Case Law 404. Case 312 validity stays HOLD / fact-dependent and must not globally stall Phase 2. One held case must not set `progress=false`. Official gap bytes may be chunked into the evaluation sidecar; do not mutate the 85-source recovery-b identity, write ACTIVE, or admit an unidentified “Mediation Act 2025”.
- Visible diagnostic 331 r1 is preserved. 331 r2 is the current evaluation continuation run. Do not mint a fresh unseen set or open the 306 private bank. Do not emit another looping locator-tick DOCX; if an official identifier is still unknown, ask the owner in chat.
- Knowledge gaps and wrong-route quotations trigger fail-closed official-source fill (`scripts/fill_ge_knowledge_gaps.py`): legislation.gov.uk / Find Case Law only, including an official exact-title Atom search when the identifier is not already known, locator-bound chunks, exact SHA-256, no gold/admission/ACTIVE/live-catalogue write. Unidentified titles stay unfetched. The local hook runs this fill when GE RESULTS are written. This is evaluation sidecar intake, not qualified legal review.
- For evaluation/training preparation, use a material factual/legal gate before mode-specific quality review. Adapt the official Law-folder 70+ standard for Essay/PB, use a plain-language practical overlay for GE, and present readable answers plus one final owner approval DOCX.
- The owner-directed full design-improvement audit selects the current object contracts in `docs/system-design/SCHEMA_REGISTRY.md`. New jobs/runs must reject legacy QueryPlan v1 and MatterFact v1, bind the full request-to-release digest chain, and use the deterministic fallback precedence in `docs/system-design/FAILURE_MODES.md`. These design contracts are not proof of implementation or run authority.

## Strict no-deletion rule

- **Never delete anything unless the owner explicitly requests that deletion and identifies its scope.** This applies to every agent and delegated task, including files, folders, source bytes, canonical text, datasets/questions, indexes, model artifacts, logs, evidence, backups, temporary outputs and Git data.
- Requests to amend, rebuild, regenerate, clean up, replace, deduplicate or reorganize are not deletion authorization. Do not run destructive cleanup, prune, unlink, truncate or database-delete operations, or move items to Trash, without an explicit owner deletion request covering those items.
- Preserve runtime, legal-source, question, evaluation and release evidence identities. The owner explicitly authorized removal of redundant system-design histories, frozen design packs and design-approval receipts on 1 September 2026; that scoped cleanup may use recoverable Trash. Working design documents may be updated in place. Never erase questions or legal/runtime evidence because they are duplicates or excluded from future scoring.
- Do not delete another agent's artifacts or bypass this rule through scripts, subprocesses, retention jobs, automatic cleanup or tool defaults. Retain temporary/rendering outputs unless the owner requests their removal.
- An authorized deletion still requires all applicable backup, restore, exact-manifest, process-stop and custody checks. Prefer recoverable Trash for approved local removal. Existing possible-cleanup permissions elsewhere in this file do not authorize deletion by themselves.

## Autonomous repair and stop policy

- Codex may make conservative technical repairs, add regressions, implement and test synthetic non-authorizing report/projection scaffolding and choose implementation mechanics that do not change an owner-approved policy, legal judgment, candidate, split or release authority.
- Only the owner may supply or approve signatures, credentials/secrets, legal-currentness judgments, model transport, private review roots, resource envelope, certification contract, 30/30 split secret, Development acceptance, promotion, O-04, Validation acceptance and live activation.
- Classify failures by a stable fingerprint: command/gate, failing test or stage, exception/error code and relevant artifact identity.
- Never rerun an unchanged failing command merely hoping for a different result. Diagnose the fingerprint and make a targeted change first.
- After the same fingerprint has failed twice despite attempted repair, stop that path before a third retry. Preserve evidence and report the blocker/root cause; do not loop or spend tokens repeating the same step.
- A distinct formatting or environment prerequisite failure is tracked separately, but it must still be corrected before rerunning the substantive gate.
- No real answer-model, Development-30, Validation-30, promotion or live invocation is a debugging tool.
- Generated gRPC stubs, UDS server/client and synthetic transport tests are technical preparation only. Production gRPC activation and model-backed conversation rewriting remain disabled until the exact Phase-2B model-transport owner gate creates the verified capability.
- Catalogue compaction and full-row classification are offline maintenance-window operations. Never run an online blind delete or in-place `VACUUM`; require a recent successful backup/restore receipt, zero writers/jobs/scans/builds, bounded diagnostics and create-new verification before any separately approved swap.
- Backup pruning requires a current successful restore drill and an exact prune manifest. Preserve at least the current recoverable backup and the most recent predecessor; use recoverable Trash for approved local cleanup rather than silent deletion.

## Review-output layout

- Development, sealed Validation custody and ordinary live review use three distinct owner-approved private roots; none may fall back to a shared canary root or another lane's root.
- Readable Development/live question-answer folders are non-authorizing projections. The immutable upstream package and signed owner decision remain authoritative.
- Codex may implement and test the folder projection with synthetic records in Phase 1. It must not invoke a real Development or live projection until the exact root is owner-approved and a typed strict verifier has accepted the exact upstream package.
- Never project or expose Sealed Validation outputs before its frozen one-pass disclosure gate.

## Clean-room rules

- Never import, copy, mount, query or fall back to code, databases, indexes, adapters, gates, configuration or runtime state from the old project.
- Do not access an old project or `LegalBot-New`, including any `New_materials` directory, without a new explicit owner authorization. `/Users/hltsang/Desktop/Law` remains the only external configured source root currently permitted.
- Raw source bytes and canonical Markdown are immutable. Search indexes are derived, versioned and disposable.
- No absolute source path, owner identifier or original personal filename may enter a prompt, answer, review JSON or application log.
- No material legal claim is released without a frozen `EvidenceSpan` whose identity, jurisdiction and currentness have passed.
- The model never renders citations. OSCOLA is deterministic from reviewed metadata.
- Teaching and feedback lanes never count as independent legal authority.
- Repairs create new versions and explicit diffs; they do not mutate or silently delete earlier prose.
- The first release binds only to `127.0.0.1`. Do not add auth, cloud storage, sharing or training exports without a new approval phase.
- Remaining selected-issue checks may use official-source internet (`legislation.gov.uk`, Find Case Law) and may download official files into configured source roots. Those bytes are not gold until catalogue or accepted v2-repair hashes match. AI is not the `legal_reviewer`.
- Run `python scripts/check_clean_room.py`, the complete Python suite and the web build/tests before promotion.
